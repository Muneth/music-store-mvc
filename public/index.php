<?php

// Requiring only one file from APP instead of requiring all the files...
 
  require_once '../app/bootstrap.php';

  // Init Core Library to fetch the url containing controller, method & parameters....
  
  $init = new Core;